apiVersion: v1
kind: Service 
metadata:
  name: service-dr
  namespace: django-react-postgres
spec:
   selector:
      app: dr
   type: LoadBalancer
   ports:
      - protocol: TCP
        port: 80
        targetPort: 80
        nodePort: 30000
